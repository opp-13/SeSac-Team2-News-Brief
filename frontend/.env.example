# frontend/CLAUDE.md §6 — api/client.ts의 BASE 상수와 일치시킬 것
VITE_API_BASE_URL=/api/v1

# CI/CD 배포 검증용 (components/common/BuildInfoBar.tsx). CI 빌드 단계에서 실제 값으로
# 덮어써야 한다 — 예: VITE_APP_VERSION=$(git rev-parse --short HEAD) npm run build
# 로컬 개발 등 값이 없으면 'dev'로 표시된다.
VITE_APP_VERSION=dev
